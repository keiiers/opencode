# OpenCode 本地維護指南 (給 Git 小白)

這份文件是為了幫助您理解如何維護這個專案而寫的。它只存在於您的電腦中，不會被上傳。

## 1. Git 是什麼？為什麼要用它？

想像 Git 是一個**時光機**加上**雲端同步硬碟**。
- **時光機**：它可以記錄檔案的每一次修改（版本），讓您可以隨時後悔，回到過去。
- **雲端同步**：它可以讓多人同時修改同一個專案，並把大家的修改合併在一起。

## 2. 核心概念

- **本地端 (Local)**：您的電腦。
- **遠端 (Remote/Upstream)**：放在 GitHub 上的官方程式碼倉庫。
- **暫存區 (Stash)**：一個臨時的抽屜，用來放您還沒做完、不想提交的修改。

## 3. 如何更新 OpenCode？ (SOP)

當官方有新功能發布時，請依照以下步驟更新：

### 第一步：把手邊的工作收起來
```bash
git stash
```
**為什麼？**
就像要鋪新桌布前，要把桌上的雜物先收進抽屜。這樣才不會跟新桌布打架（衝突）。

### 第二步：下載最新版本
```bash
git pull upstream dev
```
**為什麼？**
這就是「更新」按鈕。告訴 Git 去官方那邊把最新的修改抓下來，合併到您的電腦裡。
* `upstream`：官方倉庫的名字。
* `dev`：開發分支（大家都在這裡開發）。

### 第三步：把工作拿出來
```bash
git stash pop
```
**為什麼？**
更新完了，把剛剛收進抽屜的東西拿出來，繼續工作。

## 4. 如何跑起來？ (OpenCode 專屬)

更新完程式碼後，您需要讓它動起來：

### 安裝零件 (依賴套件)
```bash
bun install
```
**為什麼？**
程式碼通常依賴很多別人寫好的工具（套件）。這行指令會檢查缺什麼，並自動下載安裝。

### 啟動程式
```bash
bun dev
```
**為什麼？**
這會啟動開發伺服器，讓您可以看到 OpenCode 的畫面並開始使用。

## 5. 常見問題

### Q: 為什麼看到幾千個檔案變更？
**A:** 通常是因為產生了暫存檔（如 `.bun_cache`）。我們已經在 `.gitignore` 檔案中設定了黑名單，告訴 Git 忽略它們。如果再次出現，請檢查 `.gitignore`。

### Q: 這份檔案會被上傳嗎？
**A:** 不會。我們已經把它加到了 `.gitignore` 黑名單中，Git 會無視它的存在。

---
此文件僅供本地參考。
